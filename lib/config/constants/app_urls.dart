class AppUrls {
  const AppUrls._();

  ///Unlike API KEY, base url is publicly available, so there is no need to store it in .env file.
  static const String BASE_URL = "https://api.solanabeach.io/v1";
}
